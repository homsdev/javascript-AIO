let string = "Hello World";

//Get length from string 
console.log(`string.length -> returns length from a string ${string.length}`);
//toUpperCase -> return the string in uppercase
console.log(`string.toUpperCase -> returns ${string.toUpperCase()}`);
//toLowercase -> return the string in lowercase
console.log(`string.toLowercase -> return ${string.toLowerCase()}`);
//indexOf(string)->return index of first coincidence
console.log(`string.indexOf -> return ${string.indexOf("orld")} in ${string}`);
//replace(a,b)-> return a new string replacing the value a with the value b
console.log(`string.replace(a,b) ${string.replace("World","HomS")} in ${string}`);
//substring(a,b)->
console.log(`string.substring(a,b) ${string.substring(5,8)} from ${string}`);
//startsWith(value, start)->
//endsWith(value, end)->
//includes(value,start)->